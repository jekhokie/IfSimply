<div class="devise-information">
  <div class="devise-heading">Account Locked Out?</div>
  <div class="devise-sub-heading">No worries - specify your email and we'll<br/> send an unlock link!</div>

  <%= form_for(resource, :as     => resource_name,
                         :class  => "unlock-reset-form",
                         :url    => unlock_path(resource_name),
                         :remote => true) do |f| %>
    <div class="devise-errors">
      <%= flash[:alert] unless flash[:alert].blank? %>
    </div>

    <div class="unlock-reset-elements">
      <div class="email-fields">
        <%= f.label :email, "Login (email)" %>
        <%= f.email_field :email, :autofocus => true, :placeholder => "Email" %>
      </div>

      <div class="resetpass"><%= f.submit "Send Instructions", :class => "btn btn-warning unlock-reset-submit-button",
                                                               :data  => { :disable_with => "Processing..." } %></div>
    </div>
  <% end %>

  <div class="other-links">
    <%= link_to "Account not locked?",                       new_session_path(resource_name),      :remote => true %><br />
    <%= link_to "Not a member yet?",                         new_registration_path(resource_name), :remote => true %><br />
    <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name), :remote => true %><br />
    <%= link_to "Forgot your password?",                     new_password_path(resource_name),     :remote => true %><br />
  </div>

  <%= render :partial => "shared/powered_by" %>
</div>
